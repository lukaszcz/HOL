(set-info :smt-lib-version 2.7)
(set-logic QF_UFDT)
(set-info :status sat)
(declare-sort U 0)
(declare-datatype Color ((red) (blue)))
(declare-fun paint (U) Color)
(declare-const u U)
(assert (= (paint u) red))
(check-sat)
