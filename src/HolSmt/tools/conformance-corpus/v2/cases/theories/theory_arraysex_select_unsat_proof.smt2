(set-option :produce-proofs true)
(set-logic QF_AX)
(declare-sort I 0)
(declare-sort E 0)
(declare-const a (Array I E))
(declare-const i I)
(assert (not (= (select a i) (select a i))))
(check-sat)
(get-proof)
