(set-logic QF_BV)
(declare-const a (_ BitVec 8))
(declare-const b (_ BitVec 8))
(assert (and
  (bvnego a)
  (bvuaddo a b)
  (bvsaddo a b)
  (bvumulo a b)
  (bvsmulo a b)
  (bvusubo a b)
  (bvssubo a b)
  (bvsdivo a b)
))
(exit)
