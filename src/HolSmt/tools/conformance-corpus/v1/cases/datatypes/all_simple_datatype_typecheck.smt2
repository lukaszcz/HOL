(set-logic ALL)
(declare-datatype Pair ((mk-pair (left Int) (right Bool))))
(declare-datatype Color ((red) (green) (blue)))
(declare-const p Pair)
(assert (and
  ((_ is mk-pair) p)
  (= (left (mk-pair 3 true)) 3)
  (right p)
  ((_ is red) red)
))
(exit)
